{
  "name": "autodashboard",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "NEXT_TELEMETRY_DISABLED=1 next dev -p 80",
    "build": "rm -rf .next && NEXT_TELEMETRY_DISABLED=1 next build && tar $(cat package.json | jq .version)",
    "start": "NEXT_TELEMETRY_DISABLED=1 next start -p 80",
    "lint": "NEXT_TELEMETRY_DISABLED=1 next lint"
  },
  "dependencies": {
    "@fortawesome/fontawesome-svg-core": "^6.4.2",
    "@fortawesome/free-regular-svg-icons": "^6.4.2",
    "@fortawesome/free-solid-svg-icons": "^6.4.2",
    "@fortawesome/react-fontawesome": "^0.2.0",
    "next": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "autoprefixer": "latest",
    "eslint": "latest",
    "eslint-config-next": "latest",
    "postcss": "latest",
    "tailwindcss": "latest"
  }
}
